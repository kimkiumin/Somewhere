#include "physical_compass_wire.h"

// The v1 wire surface is intentionally a thin include over the portable
// protocol implementation. Host tests and the Arduino sketch compile the
// same parser, session, reassembly, and event-chunking code.
